# WiFi Credentials Template
# Copy this file to secrets.py and edit with your WiFi details
#
# IMPORTANT: secrets.py is gitignored - never commit your actual credentials!

# Primary WiFi network
WIFI_SSID = "YourNetworkName"
WIFI_PASSWORD = "YourPassword"

# Fallback WiFi network (optional - remove if not needed)
WIFI_SSID_FALLBACK = "BackupNetworkName"
WIFI_PASSWORD_FALLBACK = "BackupPassword"
